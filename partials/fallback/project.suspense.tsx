import React from "react";

export const ProjectSuspenseFallback = ({ user, data }) => {
  return (
    <div className="w-full h-full text-3xl text-white">
      PROJECT SUSPENSE FALLBACK
    </div>
  );
};
