import 'mocha';
import {assert} from 'chai';

describe('#services/project', () => {
  context('getProject', () => {
    it('should get a project by id', async () => {
      assert.isTrue(true);
    });
  });
});
