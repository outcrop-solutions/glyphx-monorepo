import {databaseTypes} from 'types';
import {Types as mongooseTypes} from 'mongoose';

export interface I{{pascalcase this.name}}Document
  extends Omit<databaseTypes.I{{pascalcase this.name}}, {{#each properties}}{{#if (isOneToOne this.relationType)}} | '{{camelcase this.name}}'{{else}}{{#if (isOneToMany this.relationType)}} | '{{camelcase this.name}}'{{/if}}{{/if}}{{/each}}> {
    {{#each properties}}
        {{#if (isOneToOne this.relationType)}}
            {{camelcase this.name}}: mongooseTypes.ObjectId;
        {{else}}
            {{#if (isOneToMany this.relationType)}}
                {{camelcase this.name}}: mongooseTypes.ObjectId[];
            {{/if}}
        {{/if}}
    {{/each}}
}
