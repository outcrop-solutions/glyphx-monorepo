// THIS CODE WAS AUTOMATICALLY GENERATED
'use client';
import useSWR from 'swr';

const use{{pluralize (pascalcase this.name)}} = () => {
  const apiRoute = `/api/{{pluralize (camelcase this.name)}}`;
  const {data, error} = useSWR(`${apiRoute}`);
  return {
    ...data,
    isLoading: !error && !data,
    isError: error,
  };
};

export default use{{pluralize (pascalcase this.name)}};