import {database as databaseTypes} from '@glyphx/types';
import {Types as mongooseTypes} from 'mongoose';

export interface I{{this.name}}CreateInput
  extends Omit<databaseTypes.I{{this.name}}, '_id' | 'createdAt' | 'updatedAt' {{#each relationships}}{{#if (isOneToOne this.type)}} | '{{stripLeadingI (lowercase this.referenceTable)}}'{{/if}}{{/each}}> {
    {{~#each relationships}}
        {{#if (isOneToOne this.type)}}{{stripLeadingI (lowercase this.referenceTable)}}: mongooseTypes.ObjectId | databaseTypes.{{this.referenceTable}};{{else}}{{#if (isOneToMany this.type)}}{{stripLeadingI (lowercase this.referenceTable)}}: (mongooseTypes.ObjectId | databaseTypes.{{this.referenceTable}})[];{{/if}}{{/if}}
    {{/each~}}
}