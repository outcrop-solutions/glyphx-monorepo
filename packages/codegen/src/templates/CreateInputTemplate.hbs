import {databaseTypes} from 'types';
import {Types as mongooseTypes} from 'mongoose';

export interface I{{pascalcase this.name}}CreateInput
  extends Omit<databaseTypes.I{{pascalcase this.name}}, '_id' | 'createdAt' | 'updatedAt' {{#each properties}}{{#if (isOneToOne this.relationType)}} | '{{camelcase this.name}}'{{else}}{{#if (isOneToMany this.relationType)}} | '{{camelcase this.name}}'{{/if}}{{/if}}{{/each}}> {
    {{#each properties}}
        {{#if (isOneToOne this.relationType)}}
            {{camelcase this.name}}: mongooseTypes.ObjectId | databaseTypes.I{{pascalcase this.referenceTable}};
        {{else}}
            {{#if (isOneToMany this.relationType)}}
                {{camelcase this.name}}: (mongooseTypes.ObjectId | databaseTypes.I{{pascalcase this.referenceTable}})[];
            {{/if}}
        {{/if}}
    {{/each}}
}
