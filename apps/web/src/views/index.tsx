/**
 * Views correspond to the content of routes contained within the pages directory.
 * They are responsible for selectively consuming application state and rendering partials within a specific layout.
 */
