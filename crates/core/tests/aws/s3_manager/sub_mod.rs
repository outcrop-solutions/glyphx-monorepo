//This is only here are an example of how to include a sub module into the integration tests.
//Once we get a little further along in our rust journy we can remove this.
    #[test]
    fn default_test2() {
        assert!(true);
    }

